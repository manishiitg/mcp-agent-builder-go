# Retired. New deployments disable this timer; the backend checks daily even
# when it has not restarted. Kept for compatibility with old release manifests.
[Unit]
Description=Legacy coding-agent CLI update timer (retired)

[Timer]
OnCalendar=*-*-* 03:30:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
