#!/usr/bin/env bash
# Retained for old deployment references. Updating shared globals in place is
# retired: the backend owns daily checks, private releases and state.json.
set -euo pipefail
printf '%s\n' 'CLI updates are managed by the AgentWorks backend. See docs/core/coding-cli-updates.md.'
