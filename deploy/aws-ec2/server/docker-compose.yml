services:
  caddy:
    build:
      context: .
      dockerfile: Dockerfile.caddy
    restart: unless-stopped
    ports: ["80:8080", "443:8443"]
    user: "10001:10001"
    extra_hosts: ["host.docker.internal:host-gateway"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - /data/video-studio/caddy-data:/data
      - /data/video-studio/caddy-config:/config
    depends_on: [frontend]

  frontend:
    image: video-studio-frontend:latest
    restart: unless-stopped
    ports: ["127.0.0.1:8085:8080"]
