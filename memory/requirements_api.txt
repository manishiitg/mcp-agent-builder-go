# Graphiti Knowledge Graph API Requirements
# Complete dependencies for API deployment with Neo4j AuraDB Cloud support

# Graphiti Core with Neo4j DB support (PyPI currently provides up to 0.20.2)
graphiti-core[neo4j]<=0.20.2

# Ollama for local embeddings
ollama>=0.1.0

# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP client for external requests
httpx>=0.25.0

# Environment variable management
python-dotenv>=1.0.0

# Data validation and serialization
pydantic>=2.5.0

# CORS middleware
python-multipart>=0.0.6

# Optional: Additional utilities
# For production deployment
gunicorn>=21.2.0

# For monitoring and logging
structlog>=23.2.0

# For API documentation
# FastAPI includes automatic OpenAPI/Swagger docs

# BGE Reranker for local cross-encoder reranking
# Note: graphiti-core[neo4j] includes both Neo4j driver and sentence-transformers
# Neo4j driver provides enterprise-grade graph database support
# sentence-transformers enables BGE reranker for improved search results
