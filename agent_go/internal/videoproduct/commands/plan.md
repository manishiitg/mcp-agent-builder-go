Show me the plan before you build it.

{{context}}

Call `show_document` for the script and the shot list so I can read them in the
Production panel. Then tell me, in plain language:

- how many paid generation calls this shot list implies, and the estimated cost;
- which model each shot uses, and why, where the choice was not obvious;
- anything you had to assume because I never said.

Then wait. Do not start generating until I say go.
