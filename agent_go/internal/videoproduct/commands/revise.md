Revise one shot without rebuilding the video.

{{context}}

Change the smallest responsible layer, per `video-editing`. Reuse the approved
narration, characters, and every other shot as they are — regenerate only what
actually has to change, and keep the revised shot on the same model and
provider its character arc is committed to.

Tell me what one regeneration will cost before you make the call. Afterwards,
re-run the quality checks on the new cut and show me the result as a new
version rather than overwriting the last one.
