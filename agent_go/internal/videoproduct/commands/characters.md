Show me every recurring character, presenter, or product in this production.

{{context}}

For each one, call `show_character` with its spec and reference image so I can
see it in the Production panel, and tell me which model and provider its whole
arc is committed to.

If a character does not have a reference image yet, say so rather than
describing it in words — the image is the thing every later shot is generated
against, so it is what I need to approve.

If shots using these characters have already been generated, tell me that too,
and how many, so I know what changing a character now would cost.
