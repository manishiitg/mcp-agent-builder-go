reports/*
bin/*
bin

generated/*
generated

# Server binary built by run_server_with_logging.sh
mcp-agent-server

# Service account credentials (contains secrets)
service_account.json

# Tool output folder - contains temporary generated code files
# These files should not be included in go mod tidy or version control
tool_output_folder/*
tool_output_folder
runs/*
runs

# Compiled binaries
/claude-code-chat
/cmd/server/server

# Example MCP server configs (may contain credentials/paths)
examples/claude-code-chat/mcp_servers.json
schema-gen
