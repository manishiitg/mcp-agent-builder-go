# Environment files - NEVER commit these!
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
.env.test
.env.staging

agent_go/configs/mcp_server_actual.json

*.log
logs/
**/logs/

bin/*

# Additional security patterns
*.key
*.pem
*.p12
*.pfx
*.crt
*.cert
*.pwd
*secret*
*credential*
*password*
!scripts/*secret*.sh
!frontend/src/**/secret*
!frontend/src/**/*ecret*
!frontend/src/api/secrets.ts
!agent_go/cmd/server/secrets_tools.go
!agent_go/cmd/family-server/secrets_store.go
!agent_go/cmd/family-server/secrets_tools.go
!agent_go/cmd/server/secrets_routes.go
!agent_go/pkg/orchestrator/**/secrets_*.go
!agent_go/cmd/server/guidance/templates/system/secret-management.md
# PLAT-272 shared workflow-secret store: source and its test, no secret values.
!agent_go/cmd/server/workflow_shared_secrets.go
!agent_go/cmd/server/workflow_shared_secrets_test.go
!agent_go/pkg/chathistory/shared_secrets.go
# The *credential* rule above is meant for credential FILES, but it also
# swallows source that merely has "Credential" in its name. These two are
# ordinary UI source (a form field and a masking helper) holding no secret
# value, and they were silently never committed: the build passed locally
# where the untracked files exist and failed in CI where they don't. Same
# class of problem the secret* negations above already had to solve.
!frontend/src/components/WorkflowProviderCredentialField.tsx
!frontend/src/utils/maskCredentialPreview.ts
# Same again for *password*: a change-your-own-password form, no password in it.
!frontend/src/components/topbar/ChangePasswordDialog.tsx

# Binary files
*.exe
*.bin
__pycache__/
*.pyc

# Test outputs
test_output.txt
aws_cost_report.txt

# Debug files
debug_*.log
*_debug.log

!*.example

agent_go/large_tool_output/*

agent_go/tool_output_folder/*

# Ignore files without extensions (executables, Go build outputs)
# Explicitly ignore common Go build outputs without extensions
agent_go/agent_go
agent_go/orchestrator
agent_go/server
agent_go/fileserver
agent_go/mcpagent-test
agent_go/mcp-agent-server

# External example build outputs
external_example/api/server
external_example/api/external_api
external_example/api/event-test
external_example/custom_logging/agent_logging

agent_go/cmd/timeout/timeout-server

agent_go/cache/*

cache/*

chat_history.db

planner-docs/

planner-docs/*

workspace-docs/
workspace-docs/*

workspace-projects/*
!workspace-projects/.gitkeep


agent_go/service-account.json
agent_go/service_account.json

# Gitleaks reports and cache
gitleaks-report.json
gitleaks-report.sarif
gitleaks-current-only.json
gitleaks-maintenance.json
.gitleaks-cache/

.cursor/
.vscode/

gcp-oauth.keys.json

./agent_go/configs/mcp_servers_clean_user.json

./agent_go/generated/*

.claude/

**/Downloads/*
Workflow/*
agent_go/Workflow/

# K8s deployment secrets
deploy/k8s/.env
workspace/workspace

# SQLite temporary files
*.db-shm
*.db-wal

# WhatsApp bot connector session state — contains Signal-protocol private
# keys; committing would let anyone take over the paired account.
agent_go/whatsapp-session.db
whatsapp-session.db

agent_go/page-*.png
agent_go/snapshot*.html
agent_go/screenshot*.png
agent_go/screenshot.png
/page-*.png
/snapshot*.html
/screenshot*.png

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
.terraformrc
terraform.rc

gws-credentials.json
agent_go/migrate-mode-names
diff.txt

# Local Go workspace (machine-specific absolute paths; CI uses go mod edit -replace)
go.work
go.work.sum

# Per-session artifacts the coding-CLI adapters project into an agent's working
# directory and delete again at session cleanup (they carry an
# "orchestrator-generated / auto-removed at session cleanup" header). They only
# survive a run that was killed before cleanup, and they should never appear in
# the repo at all — a run that writes them HERE means something passed the wrong
# working directory (see enginedetect.Chat, where codex-cli used to drop it).
# Ignored as a backstop so a regression can't commit them; if you ever want a
# real, hand-written AGENTS.md tracked, force-add it and narrow this line.
AGENTS.md
.codex/
.local/
transcript.txt
