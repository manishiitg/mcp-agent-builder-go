name: Desktop DMG

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-dmg-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  commit-artifact:
    if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
    runs-on: macos-15-intel
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Checkout MCPAgent dependency
        uses: actions/checkout@v4
        with:
          repository: manishiitg/mcpagent
          path: deps/mcpagent

      - name: Checkout LLM provider dependency
        uses: actions/checkout@v4
        with:
          repository: manishiitg/llm-provider-mcp
          path: deps/multi-llm-provider-go

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            desktop/package-lock.json

      - name: Install Frontend Dependencies
        run: |
          set -euo pipefail
          cd frontend
          npm ci

      - name: Test Frontend Performance Contracts
        run: |
          set -euo pipefail
          cd frontend
          npm test

      - name: Build Frontend Static Assets
        run: |
          set -euo pipefail
          cd frontend
          npm run build

      - name: Copy Frontend Into Agent Static Directory
        run: |
          set -euo pipefail
          rm -rf agent_go/static
          mkdir -p agent_go/static
          cp -R frontend/dist/. agent_go/static/

      - name: Use Checked Out Go Module Dependencies
        run: |
          set -euo pipefail
          cd agent_go
          go mod edit -replace=github.com/manishiitg/mcpagent=../deps/mcpagent
          go mod edit -replace=github.com/manishiitg/multi-llm-provider-go=../deps/multi-llm-provider-go
          go mod download

      - name: Build Agent Server (arm64)
        run: |
          set -euo pipefail
          # cgo on purpose: the agent links the shared speech engine's native
          # libraries (pkg/voicestt); the script stages them in resources/lib.
          bash scripts/build-darwin-voice-binary.sh agent_go desktop/resources/agent-server arm64

      - name: Build Workspace Server (arm64)
        run: |
          set -euo pipefail
          cd workspace
          go mod download
          GOOS=darwin GOARCH=arm64 go build -o ../desktop/resources/workspace-server .

      - name: Install Desktop Dependencies
        run: |
          set -euo pipefail
          cd desktop
          npm ci

      - name: Build Commit Artifact
        run: |
          set -euo pipefail
          cd desktop
          npx electron-builder --mac --arm64 --publish never

      - name: Upload DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AgentForge-${{ github.run_number }}-${{ github.sha }}-mac-arm64
          path: |
            desktop/dist/*.dmg
            desktop/dist/*.zip
          if-no-files-found: error
          retention-days: 14

  release:
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    runs-on: macos-15-intel
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Checkout MCPAgent dependency
        uses: actions/checkout@v4
        with:
          repository: manishiitg/mcpagent
          path: deps/mcpagent

      - name: Checkout LLM provider dependency
        uses: actions/checkout@v4
        with:
          repository: manishiitg/llm-provider-mcp
          path: deps/multi-llm-provider-go

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            desktop/package-lock.json

      - name: Install Frontend Dependencies
        run: |
          set -euo pipefail
          cd frontend
          npm ci

      - name: Test Frontend Performance Contracts
        run: |
          set -euo pipefail
          cd frontend
          npm test

      - name: Build Frontend Static Assets
        run: |
          set -euo pipefail
          cd frontend
          npm run build

      - name: Copy Frontend Into Agent Static Directory
        run: |
          set -euo pipefail
          rm -rf agent_go/static
          mkdir -p agent_go/static
          cp -R frontend/dist/. agent_go/static/

      - name: Use Checked Out Go Module Dependencies
        run: |
          set -euo pipefail
          cd agent_go
          go mod edit -replace=github.com/manishiitg/mcpagent=../deps/mcpagent
          go mod edit -replace=github.com/manishiitg/multi-llm-provider-go=../deps/multi-llm-provider-go
          go mod download

      - name: Build Agent Server (arm64)
        run: |
          set -euo pipefail
          # cgo on purpose: the agent links the shared speech engine's native
          # libraries (pkg/voicestt); the script stages them in resources/lib.
          bash scripts/build-darwin-voice-binary.sh agent_go desktop/resources/agent-server arm64

      - name: Build Workspace Server (arm64)
        run: |
          set -euo pipefail
          cd workspace
          go mod download
          GOOS=darwin GOARCH=arm64 go build -o ../desktop/resources/workspace-server .

      - name: Install Desktop Dependencies
        run: |
          set -euo pipefail
          cd desktop
          npm ci

      - name: Build & Publish Release
        run: |
          set -euo pipefail
          cd desktop
          # Sync package.json version with git tag (strip 'v' prefix).
          # Use npm pkg set which is idempotent (npm version errors when target == current).
          VERSION=${GITHUB_REF#refs/tags/v}
          CURRENT=$(node -p "require('./package.json').version")
          echo "Tag version: $VERSION, package.json version: $CURRENT"
          if [ "$CURRENT" != "$VERSION" ]; then
            npm version "$VERSION" --no-git-tag-version
          else
            echo "package.json already at $VERSION; skipping version bump"
          fi

          npm run dist -- --arm64 --publish always

          # Versions through v1.25.111 construct the updater URL with the old
          # product name. Keep this alias so those installations can jump
          # directly to the latest AgentWorks release.
          cp "dist/AgentWorks-${VERSION}-arm64.dmg" "dist/Runloop-${VERSION}-arm64.dmg"
          gh release upload "v${VERSION}" "dist/Runloop-${VERSION}-arm64.dmg" --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
