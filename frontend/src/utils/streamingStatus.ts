// Moved to the shared session client so every product surface (AgentWorks,
// Video Studio, SparkQuill) classifies streaming chunks with the same rules.
export * from '../../shared/session/streamingStatus'
